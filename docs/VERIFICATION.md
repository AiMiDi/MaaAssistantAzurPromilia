# 验证记录

2026-09-20，Windows x64，bilibili PC 中文版，游戏画面 1920×1080，MaaFramework 5.13.1，MFAAvalonia 2.16.1。

## 真机记录

- 通过 FramePool 捕获游戏画面，归一化为 1280×720。
- 邮件实际奖励已领取；`ClaimMail` 完整流程再次运行，识别“附件已全部领取”并返回菜单。
- 来自星海的礼赠第 3 天奖励已实际领取；`ClaimSignIn` 完整流程再次运行，通过连续三帧确认当前没有可领取卡片后退出。
- 家园一次收获同时得到生产建筑、农田和家园采集产物；处理过家园等级提升弹窗。
- 惊奇爆谷实际制作 4 份，消耗玉米 24 个；成品随后通过一键收获取回，并参与餐桌一键添加。
- 小份波波饼实际自动排入 3 份，消耗小麦 12 个；完成后截图确认 `3/3`。随后完整家园流程成功收取并一键补餐，餐桌显示 513/1400。
- 复测发现配方卡片排列改变，修正为通过列表中的配方名称定位；此前和当前两种排列均通过截图识别回归。材料数字区域避开图标，`13/4`、`28/6`、`28/24` 均正确识别。
- 完成状态的金色数量曾使旧 OCR 失败，已增加独立“制作完成”识别并收窄队列数量区域。完成截图命中完成状态、未命中空闲状态；空队列截图结果相反。无法确认队列状态时停止，不新增制作。
- `HomeRoutine` 命令行完整流程通过：收获 → 检查制作 → 一键补餐 → 返回家园中枢。
- MFAAvalonia 真实启动 Agent 并执行 `HomeRoutine` 通过；验证了两个候选均缺材料、没有料理可添加时的明确空结果。
- GUI 再次运行验证候选优先顺序修改生效，并完成新增料理的一键补餐。置顶标志实测：运行前 `False`，运行中 `True`，任务完成后恢复 `False`。
- 日常登录任务已实际领取；20 活跃度宝箱通过点击中心领取，出现奖励结算，退出后可领取红点消失。领取前/后截图验证了识别状态变化。
- `ClaimDaily` 命令行完整流程通过：切换日常页 → 滚动回顶部 → 检查至底部 → 检查五档宝箱 → 返回菜单。本次剩余可领奖励为 0；不等同于所有档位正向领取均已验证。
- MFAAvalonia 通过自身任务队列执行 `ClaimDaily` 成功，耗时约 18 秒，日志最终状态为 `SUCCEEDED`，与命令行的空结果一致。

原始截图与日志仅保存在本地 `debug/`、`install/logs/`，包含账号信息，不上传。

## 自动检查

`pytest` 的 31 项检查覆盖错误画幅和黑屏拒绝、原生回调的 JSON 类型、数量 OCR 歧义、未知页面不输入、输入节点的画幅前置条件、候选排序与排除、材料不足的候选切换、保留已有队列、单份制作策略、未知队列不新建制作、完成提示替代模糊数量识别、正常/异常结束后的置顶恢复，以及日常列表遍历、领取后重排复查、滚动上限、宝箱活跃度门槛和领取结果校验。

官方 JSON Schema 验证全部 Pipeline 和 Project Interface 配置。

## 尚未证明的范围

- 不同账号的家园建筑布局，其他语言及非 16:9 画幅。
- 其他 16:9 分辨率的真机效果；归一化架构已具备，不能等同于逐分辨率实测。
- 跨天签到、满餐桌及完全无收获产物等状态。
- 食物制作完成等待的完整新队列链路：已有真实分步制作和收取记录，自动等待路径另有状态测试；需下一批材料到位后继续实测。
- 40/60/80/100 活跃度宝箱的实际领取，以及达到 100 活跃度后再领任务的状态变化；这些分支仍需达标样本验证。

本项目仍为开发版。任务遇到未识别状态会失败并保留页面，日志中的“没有可填充料理”不表示本轮增加了饱腹值。
