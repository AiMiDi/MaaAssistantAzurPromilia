# 验证记录

2026-09-22 本轮收尾：96 项单元测试通过，官方 Pipeline／Interface Schema 通过。`HomeOrders` 实际新增提交一张订单，加上此前原子节点提交的一张，共两张兽灵之角订单、硬币 +10000；复跑提交 0 项，斑红菇缺 76、兽灵之角缺 10 的订单保留。实际制作 1 个苗鸡木雕，完成动画造成数量 OCR 短暂失败后，恢复运行先收获，确认库存为 1，没有再制作；该瞬态已加有界重试。

小麦饼单独补餐实测：饱腹值 525 → 1800/1800，饼干背包 768 → 717、餐桌 21 → 72，其他三种食物数量不变。`HomeFeed` 满桌分支通过，`HomeRoutine` 新版生产收获与牧场检查连跑通过，餐桌已满时跳过制作。当前缺口数量和种子换算有单元测试；本次满桌，不额外消耗材料制造缺口，不将此算作缺料补种的实测。

2026-09-22 多设施制作：两座工作台分别排入迷你狼神石俑和密文环饰，恢复收获后再次检查目标库存均已满足，没有重复制作。修正建筑列表滚动位置后，完整自动补做凝质晶球 1 个并收获，库存验证为 1（`debug/production_make3.log`）。六种商品配方均能读取，软绒围巾缺月麻 1 个时保留并报告。两座工作台和两座熔炉均识别成功；木炭原料显式选择软木并核对每份消耗 3 个。库存缓冲与跨设施递归合成通过单元测试；本次仓库中间原料充足，未故意耗尽库存验证整条合成链。

熔炉小批量实测：两次各补木炭 2 个，均先给两座熔炉分别排入 1 个；返回等待时暴露第三排状态栏遮挡及滚动惯性问题。缩短滚动距离并用相同建筑编号校准后，恢复流程逐座确认完成、自动收获并核验木炭库存 300（原 296，累计只新增 4 个；`debug/resume_furnaces.log`）。这是“排入成功 + 修复后恢复收获”的分段验证，不冒充修复后的单次端到端正向记录。

修复后完整熔炉验证通过：同一运行先向两座熔炉分别排入尘石砖 1 个、风铜锭 1 个，再逐座等待、自动收获并读回库存，尘石砖 615 → 616，风铜锭 406 → 407（`debug/verify_furnaces.log`，`succeeded: true`）。最终六商品预览也通过：五种商品满足目标，软绒围巾缺月麻 1，没有新增制作（`debug/production_final_preview.log`）。

同日 MFAAvalonia 最终日常与签到队列通过（03:13:48 `SUCCEEDED`），随后恢复用户原有任务勾选，新制作任务默认仅预览。

2026-09-22：`ClaimSignIn` 真机领取成功（`debug/signin_current.json`），第二次运行识别无可领取卡片（`debug/signin_repeat.json`）；签到页面截图显示奖励卡片已打勾。补充奖励弹窗处理及退出后返回菜单，完整更新版已从种子仓库起始页面回归成功（`debug/signin_final.log`）。奖励弹窗分支仅单元验证，本次签到为直接卡片状态变化。

同日：奇波牧场篮子实际点击后出现带“牧场”标题的收获清单，关闭后篮子消失（`debug/ranch_before.png`、`ranch_result.png`、`ranch_after.png`）。已接入 `collect()`，覆盖没有篮子、牧场清单确认、升级弹窗及篮子持续存在时不重复点击的测试。生产与牧场连跑已通过，连跑时牧场为无篮子分支。

启动：MFAAvalonia 从游戏进程不存在开始，通过官方启动器打开游戏并自动进入可识别页面，两次日志记录 `SUCCEEDED`。资源更新确认规则通过用户提供弹窗截图的离线回放，移除更新文字的负样本不点击；尚无本次现场资源下载确认的正向证据。

订单：672 条 Wiki 需求已结构化导入，静态校验覆盖需求变体、未知库存、材料保留量与错误数量。数据导入和单元测试不等同于实际订单提交成功。

2026-09-21 Wiki 准备阶段：用户占用电脑期间未操作游戏。离线资料含 86 个结构化配方、15 个待补产出 ID 的装备材料表、154 个系统详情页及 22 个商品 PNG；索引数量、JSON 清单哈希和图标哈希校验通过。`pytest` 当前 44 项通过，新增库存扣除、保留量、依赖排序、共享原料、批次、缺料、未知库存、可选材料、循环依赖和 Wiki 解析边界检查；官方 Pipeline 与 Interface Schema 通过。命令行离线示例正确输出“软木板 4 → 苗鸡木雕 2”，不连接游戏。工作台商品页面的已观察原子节点已整理，目标制作尚未接入 GUI，也没有将离线测试算作游戏制作成功。

2026-09-20，Windows x64，bilibili PC 中文版，游戏画面 1920×1080，MaaFramework 5.13.1，MFAAvalonia 2.16.1。

2026-09-21 补充：从种子仓库页面开始，`HomeRoutine` 自动返回、收获新材料、创建小份波波饼队列、等待制作完成、再次收取并一键补餐，全程无人工接管，最终状态 `succeeded: true`。餐桌由 513 增至 588/1400。证据保存在本地 `debug/home_from_seeds.json` 与对应截图。修正后的新队列完整链路已通过。

## 真机记录

- 通过 FramePool 捕获游戏画面，归一化为 1280×720。
- 邮件实际奖励已领取；`ClaimMail` 完整流程再次运行，识别“附件已全部领取”并返回菜单。
- 来自星海的礼赠第 3 天奖励已实际领取；`ClaimSignIn` 完整流程再次运行，通过连续三帧确认当前没有可领取卡片后退出。
- 家园一次收获同时得到生产建筑、农田和家园采集产物；处理过家园等级提升弹窗。
- 惊奇爆谷实际制作 4 份，消耗玉米 24 个；成品随后通过一键收获取回，并参与餐桌一键添加。
- 小份波波饼实际自动排入 3 份，消耗小麦 12 个；完成后截图确认 `3/3`。随后完整家园流程成功收取并一键补餐，餐桌显示 513/1400。
- 复测发现配方卡片排列改变，修正为通过列表中的配方名称定位；此前和当前两种排列均通过截图识别回归。材料数字区域避开图标，`13/4`、`28/6`、`28/24` 均正确识别。
- 完成状态的金色数量曾使旧 OCR 失败，已增加独立“制作完成”识别并收窄队列数量区域。完成截图命中完成状态、未命中空闲状态；空队列截图结果相反。无法确认队列状态时停止，不新增制作。
- `HomeRoutine` 命令行完整流程通过：收获 → 检查制作 → 一键补餐 → 返回家园中枢。
- MFAAvalonia 真实启动 Agent 并执行 `HomeRoutine` 通过；验证了两个候选均缺材料、没有料理可添加时的明确空结果。
- GUI 再次运行验证候选优先顺序修改生效，并完成新增料理的一键补餐。置顶标志实测：运行前 `False`，运行中 `True`，任务完成后恢复 `False`。
- 日常登录任务已实际领取；20 活跃度宝箱通过点击中心领取，出现奖励结算，退出后可领取红点消失。领取前/后截图验证了识别状态变化。
- `ClaimDaily` 命令行完整流程通过：切换日常页 → 滚动回顶部 → 检查至底部 → 检查五档宝箱 → 返回菜单。本次剩余可领奖励为 0；不等同于所有档位正向领取均已验证。
- MFAAvalonia 通过自身任务队列执行 `ClaimDaily` 成功，耗时约 18 秒，日志最终状态为 `SUCCEEDED`，与命令行的空结果一致。

原始截图与日志仅保存在本地 `debug/`、`install/logs/`，包含账号信息，不上传。

## 自动检查

`pytest` 的 31 项检查覆盖错误画幅和黑屏拒绝、原生回调的 JSON 类型、数量 OCR 歧义、未知页面不输入、输入节点的画幅前置条件、候选排序与排除、材料不足的候选切换、保留已有队列、单份制作策略、未知队列不新建制作、完成提示替代模糊数量识别、正常/异常结束后的置顶恢复，以及日常列表遍历、领取后重排复查、滚动上限、宝箱活跃度门槛和领取结果校验。

官方 JSON Schema 验证全部 Pipeline 和 Project Interface 配置。

## 尚未证明的范围

- 不同账号的家园建筑布局，其他语言及非 16:9 画幅。
- 其他 16:9 分辨率的真机效果；归一化架构已具备，不能等同于逐分辨率实测。
- 跨天签到、满餐桌及完全无收获产物等状态。
- 40/60/80/100 活跃度宝箱的实际领取，以及达到 100 活跃度后再领任务的状态变化；这些分支仍需达标样本验证。

本项目仍为开发版。任务遇到未识别状态会失败并保留页面，日志中的“没有可填充料理”不表示本轮增加了饱腹值。
